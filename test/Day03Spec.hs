-- | Tests for "Day03". Currently a pending placeholder; replace the
-- 'pendingWith' bodies with real assertions when the day is solved.

module Day03Spec (spec) where

import Test.Hspec

spec :: Spec
spec = describe "Day 03" $ do
  describe "part1" $
    it "matches the example" $
      pendingWith "AoC 2018 Day 03 not yet attempted"
  describe "part2" $
    it "matches the example" $
      pendingWith "AoC 2018 Day 03 not yet attempted"
